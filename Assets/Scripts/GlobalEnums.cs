public enum Direction
{
    up,
    down,
    left,
    right
}

public enum State
{
    idle,
    running,
    jumping,
    dashing,
    attacking
}

public enum RecoilDir
{
    up,
    down,
    left,
    right
}